# 企业实训最终答辩提交说明

> 《软件工程管理与实践》· 企业实训项目
> 项目：农机作业数据处理与分析软件（基于 Django）

## 一、内容清单

| 内容 | 位置 |
|---|---|
| 主系统源码（含数据库，开箱即用） | `agricultural-machinery-system/` |
| 自动化测试工程（单元 + 集成 + E2E，共 32 例） | `农机系统自动化测试/` |
| 最终答辩 PPT | `农机作业最终答辩.pptx` |

本提交包同步托管于 GitHub（可在线浏览源码与提交历史）：
<https://github.com/zhaowinter23/agricultural-machinery-final-submission>

## 二、快速运行主系统

运行环境：Python 3.10 及以上，Windows / macOS 均可。

```
cd agricultural-machinery-system
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
python manage.py runserver
```

浏览器访问 <http://127.0.0.1:8000>，默认账号：

| 角色 | 用户名 | 密码 |
|---|---|---|
| 管理员（可导入/删除数据、管理用户） | admin | admin123 |
| 普通用户（查看地图、查询、统计） | user | user123 |

数据库 `db.sqlite3` 已随附全部数据（8 个地块、43,753 个轨迹点），**无需导入，启动即可用**。
详细图文步骤（Python 安装、虚拟环境、常见问题排查）见 `agricultural-machinery-system/README.md`。

## 三、运行自动化测试

先安装依赖（测试工程会 import 主系统，故主系统依赖也需安装）：

```
cd 农机系统自动化测试
pip install -r ../agricultural-machinery-system/requirements.txt
pip install -r requirements.txt
python -m playwright install chromium      # 仅 E2E 用例需要，跑前需执行一次
```

运行：

```
python -m pytest -m "not e2e"    # 单元 + 集成，28 例，约 5 秒，无需浏览器
python -m pytest                 # 全部 32 例（E2E 地图用例依赖联网加载 Leaflet CDN）
```

> 注意：`农机系统自动化测试/` 与 `agricultural-machinery-system/` 需保持同级且目录名不变（测试通过相对路径定位主系统）。

## 四、提交前自测记录（2026-09-18）

- 主系统：提交包内全新副本启动成功，管理员登录、首页渲染验证通过（自带数据库开箱即用）
- 自动化测试：32 / 32 通过（单元 14 + 集成 14 + E2E 4，约 19 秒），E2E 覆盖登录流、统计页、查询页、地图交互
